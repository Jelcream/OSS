keytool -exportcert -alias androiddebugkey -keystore “C:\Users\USER\.android\debug.keystore” -storepass android -keypass android | openssl sha1 -binary | openssl base64

원격저장소는 여러명이 쓸 수 있어서 
바뀌면 안된다. 
원격 저장소 데이터를 수동으로 바꿔줘야 한다. 

update 1+1+1
업데이트합니다. 
reverse 되나 확인 

